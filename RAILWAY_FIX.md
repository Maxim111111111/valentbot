# ✅ Как ПРАВИЛЬНО добавить переменные окружения в Railway

## 🔴 Неправильный способ (переменные сбрасываются)
❌ Добавлять переменные в случайном порядке  
❌ Редактировать через консоль  
❌ Забывать нажимать "Save"  
❌ Добавлять в неправильный сервис  

## ✅ Правильный способ (переменные сохраняются)

### Шаг 1: Откройте GitHub репозиторий в Railway

1. Откройте https://railway.app
2. Нажмите на ваш **Project** → **Deployments**
3. Нажмите на Web Service (название вашего приложения)

### Шаг 2: Перейдите в Variables (основное место для переменных)

1. В меню сверху найдите вкладку **Variables**
2. **НЕ НАЖИМАЙТЕ на Settings**, там может быть другое место

### Шаг 3: Добавьте все переменные разом

Скопируйте и добавьте все эти переменные **вместе**:

```
TELEGRAM_BOT_TOKEN=8294908477:AAGdjRo5ucowozIT81qGmMJ5vP8R0YAYDNM
TELEGRAM_BOT_USERNAME=valentinmvbot
CLOUDINARY_CLOUD_NAME=dvzwzytog
CLOUDINARY_API_KEY=396524255276334
CLOUDINARY_API_SECRET=7XHmrXMN2ZfcIZcJW58o0wINd68
NODE_ENV=production
```

### Шаг 4: Для DATABASE_URL

**Вариант A (если PostgreSQL плагин уже добавлен):**
- Railway автоматически создаст `PGHOST`, `PGUSER`, `POSTGRES_PASSWORD`, `PGDATABASE`
- Приложение ссамо их соберет

**Вариант B (добавить DATABASE_URL вручную):**
1. Перейдите в PostgreSQL плагин (в том же проекте)
2. Скопируйте **Public URL** из PostgreSQL вкладки
3. Вставьте в Web Service → Variables как:
```
DATABASE_URL=postgresql://postgres:password@host:5432/railway
```

### Шаг 5: Для APP_URL

1. После первого deploy откройте вашу веб-страницу
2. Скопируйте URL из браузера (например: `https://valentbot-production-xxxx.up.railway.app`)
3. Добавьте переменную:
```
APP_URL=https://valentbot-production-xxxx.up.railway.app
```

### Шаг 6: Сохраняем и перезагружаем

1. После добавления каждого набора переменных нажимайте **Deploy** или **Redeploy**
2. ⏳ Дождитесь завершения (5-10 минут)
3. Проверьте логи в **Deployment** → **Logs**

---

## 🔍 Как проверить что переменные сохранились

1. Откройте Web Service
2. Вкладка **Variables** 
3. Проверьте что все переменные **видны и сохранены**
4. Откройте логи → ищите `DATABASE_URL constructed` или `✅ Database connected`

---

## 🚨 Если переменные всё равно сбрасываются

### Способ 1: Проверьте что добавляете в правильное место
- 🔴 Settings → Переменные = неправильно
- ✅ Variables вкладка = правильно

### Способ 2: Пересоздайте сервис
1. Удалите существующий Web Service
2. Создайте заново через Deploy from GitHub
3. Добавьте переменные в Variables

### Способ 3: Используйте Railway CLI
```bash
# Установите Railway CLI
npm install -g @railway/cli

# Авторизуйтесь
railway login

# Свяжитесь с проектом
railway link

# Добавьте переменные
railway variables set TELEGRAM_BOT_TOKEN=8294908477:AAGdjRo5ucowozIT81qGmMJ5vP8R0YAYDNM
railway variables set NODE_ENV=production
# ... и т.д.

# Проверьте
railway variables
```

### Способ 4: Используйте .env файл для локальной разработки apenas

Не добавляйте .env в репозиторий! Добавлено в .gitignore.

---

## 📋 Финальный чек-лист

- [ ] Вошли в Railway проект
- [ ] Открыли Web Service (не PostgreSQL)
- [ ] Перешли в вкладку **Variables**
- [ ] Добавили все переменные
- [ ] Нажали **Redeploy**
- [ ] Дождались завершения (5-10 минут)
- [ ] Проверили логи - нет ошибок
- [ ] Откыли публичный URL - приложение работает

---

## 📞 Если всё равно не работает

1. Откройте **Deployment** → **Logs**
2. Ищите строку с ошибкой
3. Проверьте есть ли `undefined` - значит переменная не добавлена
4. Убедитесь что переменная добавлена в **Variables**, а не в Settings

## 💡 Совет

**Сделайте скриншот Variables после добавления всех переменных** - так вы увидите что они точно сохранены!
