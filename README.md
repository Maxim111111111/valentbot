# Valentine Game - Telegram Web App

Интерактивное приложение для создания валентинок с мини-игрой.

## 🚀 Развертывание на Railway

### 1. Подготовка переменных окружения

В Railway добавьте следующие переменные окружения:

```
TELEGRAM_BOT_TOKEN=<ваш_токен>
TELEGRAM_BOT_USERNAME=valentinmvbot
CLOUDINARY_CLOUD_NAME=dvzwzytog
CLOUDINARY_API_KEY=396524255276334
CLOUDINARY_API_SECRET=7XHmrXMN2ZfcIZcJW58o0wINd68
APP_URL=https://ваш-домен.up.railway.app
NODE_ENV=production
```

### 2. Создание PostgreSQL базы на Railway

1. Добавьте PostgreSQL плагин в Railway
2. Скопируйте значения PGUSER, POSTGRES_PASSWORD, POSTGRES_DB
3. DATABASE_URL будет автоматически установлен

### 3. Deployment

1. Подключите репозиторий к Railway
2. Railway автоматически установит зависимости и запустит приложение

## 📱 Функции

- ✨ Создание валентинок с медиа
- 🎮 Мини-игра на 25 секунд
- 🏆 Глобальный рейтинг
- 💌 Поделиться в Telegram

## 🛠️ Локальная разработка

### Требования

- Node.js 16+
- PostgreSQL
- npm

### Установка

```bash
npm install
cd frontend && npm install
```

### Запуск

```bash
# Backend
npm start

# Frontend (в отдельном терминале)
cd frontend
npm run dev
```

Backend будет доступен на `http://localhost:3000`  
Frontend на `http://localhost:5173`

## 📦 Структура проекта

```
├── server.js              # Основной файл сервера
├── src/
│   ├── config/           # Конфигурация БД и Cloudinary
│   ├── models/           # Sequelize модели
│   ├── controllers/      # API контроллеры
│   ├── routes/           # API маршруты
│   └── middleware/       # Middleware
├── frontend/
│   ├── src/
│   │   ├── pages/        # Компоненты экранов
│   │   ├── App.jsx       # Главное приложение
│   │   └── App.css       # Стили
│   └── vite.config.js    # Конфиг Vite
└── package.json          # Зависимости
```

## 🔗 API Endpoints

- `POST /api/cards` - Создать валентинку
- `GET /api/cards/:id` - Получить валентинку
- `POST /api/game-result` - Сохранить результат игры
- `GET /api/top` - Получить топ-10 рейтинг
- `GET /api/rank` - Получить ранг игрока

## 🎮 Механика игры

- ⏱️ Таймер: 25 секунд
- ❤️ Красное сердце: +1 очко
- 💛 Золотое сердце: +3 очка
- 🏆 Результаты сохраняются в БД

## 🚀 Оптимизация для Railway

- Автоматическое построение фронтенда
- Использование только необходимых зависимостей
- PostgreSQL для хранения данных
- Cloudinary для медиа-файлов
- Минимальные требования к ресурсам

## 📝 Лицензия

MIT
